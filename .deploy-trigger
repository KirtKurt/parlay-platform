Deploy trigger for current main.
Purpose: deploy MLB complete-slate, doubleheader-safe lock coverage and non-backfillable daily lock.
Timestamp UTC: 2026-07-11T21:50:00Z
Required live proof: manifest game count = prediction count = stored count; coverageComplete=true; operationalDefect=false.
Required daily lock policy: use all pre-lock pulls, preserve doubleheaders by provider id/start time, and never create a full-slate lock after the first game starts.
Supersedes the prior latest-pull-only daily lock and matchup-only audit identity.
