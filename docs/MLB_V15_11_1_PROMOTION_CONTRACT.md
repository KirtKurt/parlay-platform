# Promotion contract

Production promotion is forbidden until a real historical-data run proves all mandatory conditions. The production cutover is write-once, removes legacy selection and fallback authority, fails closed if the approved artifact is missing or mismatched, and never enables automatic wagering.
