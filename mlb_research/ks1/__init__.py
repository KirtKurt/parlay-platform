"""Vendored KS1 feature dependencies for the isolated MLB research Lambda.

The authoritative implementations live under repository-root ``ks1``. Tests
require the vendored modules to remain byte-identical so the MLB-only CodeUri
can import them without widening the Lambda package to unrelated sports.
"""
