name: Reconcile MLB prospective backlog v3 once

on:
  push:
    branches: [main]
    paths:
      - "scripts/reconcile_mlb_prospective_backlog_v3.py"
      - "tests/unit/test_reconcile_mlb_prospective_backlog_v3.py"
      - ".github/workflows/mlb-prospective-backlog-reconcile-v3-once.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: parlay-platform-mlb-prospective-backlog-reconcile-v3
  cancel-in-progress: false

jobs:
  reconcile-and-train:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact source commit
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Install verification runtime
        run: python -m pip install --quiet "boto3>=1.34,<2" "pytest>=8,<9"

      - name: Verify official-status authority wrapper
        run: |
          set -euo pipefail
          python -m py_compile \
            scripts/reconcile_mlb_prospective_backlog.py \
            scripts/reconcile_mlb_prospective_backlog_v3.py
          python -m pytest -q \
            tests/unit/test_reconcile_mlb_prospective_backlog.py \
            tests/unit/test_reconcile_mlb_prospective_backlog_v3.py \
            tests/unit/test_mlb_prospective_backlog_workflow.py

      - name: Reconcile bounded backlog under official status authority
        run: |
          set -euo pipefail
          python scripts/reconcile_mlb_prospective_backlog_v3.py \
            --stack-name parlay-platform-dev \
            --region "${{ secrets.AWS_REGION }}" \
            --max-slate-days 14 \
            --output runtime_reports/mlb_prospective_backlog_v3_latest.json

      - name: Invoke trainer and require accepted prospective rows
        env:
          AWS_MAX_ATTEMPTS: "1"
        run: |
          set -euo pipefail
          TRAINER_ARN=$(aws cloudformation describe-stack-resource \
            --stack-name parlay-platform-dev \
            --logical-resource-id MLBMLTrainingFunction \
            --region "${{ secrets.AWS_REGION }}" \
            --query 'StackResourceDetail.PhysicalResourceId' \
            --output text)
          test -n "$TRAINER_ARN"
          test "$TRAINER_ARN" != "None"
          python scripts/invoke_mlb_trainer_with_retry.py \
            --function-name "$TRAINER_ARN" \
            --region "${{ secrets.AWS_REGION }}" \
            --payload '{"sport":"mlb","mode":"scheduled","run":"prospective_backlog_v3_reconciled_training"}' \
            --response /tmp/mlb-prospective-v3-training.json \
            --invocation /tmp/mlb-prospective-v3-training-invoke.json \
            --retry-execution-lease \
            --deadline-seconds 1200 \
            --retry-delay-seconds 20
          python scripts/invoke_mlb_trainer_with_retry.py \
            --function-name "$TRAINER_ARN" \
            --region "${{ secrets.AWS_REGION }}" \
            --payload '{"sport":"mlb","mode":"selection_capture","run":"prospective_backlog_v3_reconciled_selection_capture"}' \
            --response /tmp/mlb-prospective-v3-selection.json \
            --invocation /tmp/mlb-prospective-v3-selection-invoke.json \
            --retry-execution-lease \
            --deadline-seconds 1200 \
            --retry-delay-seconds 20
          python scripts/invoke_mlb_trainer_with_retry.py \
            --function-name "$TRAINER_ARN" \
            --region "${{ secrets.AWS_REGION }}" \
            --status-training-result /tmp/mlb-prospective-v3-training.json \
            --status-selection-capture-result /tmp/mlb-prospective-v3-selection.json \
            --response /tmp/mlb-prospective-v3-status.json \
            --invocation /tmp/mlb-prospective-v3-status-invoke.json
          python - <<'PY'
          import json
          from pathlib import Path

          training = json.loads(Path('/tmp/mlb-prospective-v3-training.json').read_text(encoding='utf-8'))
          status = json.loads(Path('/tmp/mlb-prospective-v3-status.json').read_text(encoding='utf-8'))
          latest = ((status.get('trainingHealth') or {}).get('latestRun') or {})
          accepted = latest.get('acceptedRowCount', training.get('acceptedRowCount'))
          try:
              accepted = int(accepted)
          except (TypeError, ValueError):
              accepted = 0
          evidence = {
              'ok': accepted > 0,
              'acceptedRowCount': accepted,
              'rejectedRowCount': latest.get('rejectedRowCount', training.get('rejectedRowCount')),
              'status': latest.get('status', training.get('status')),
              'canonicalSlateContinuity': latest.get('canonicalSlateContinuity', training.get('canonicalSlateContinuity')),
              'partitionCounts': latest.get('partitionCounts', training.get('partitionCounts')),
              'rejectionReasonCounts': latest.get('rejectionReasonCounts', training.get('rejectionReasonCounts')),
              'modelTrained': latest.get('modelTrained', training.get('modelTrained')),
              'liveInferenceAuthority': latest.get('liveInferenceAuthority', training.get('liveInferenceAuthority')),
              'productionAuthorityChanged': latest.get('productionAuthorityChanged', training.get('productionAuthorityChanged')),
          }
          Path('runtime_reports/mlb_prospective_rows_v3_latest.json').write_text(
              json.dumps(evidence, indent=2, sort_keys=True, default=str) + '\n',
              encoding='utf-8',
          )
          print(json.dumps(evidence, indent=2, sort_keys=True, default=str))
          if accepted <= 0:
              raise SystemExit('MLB prospective trainer still has zero accepted rows after official-authority reconciliation')
          if evidence['liveInferenceAuthority'] is True:
              raise SystemExit('Prospective repair unexpectedly activated live inference authority')
          if evidence['productionAuthorityChanged'] is True:
              raise SystemExit('Prospective repair unexpectedly changed production authority')
          PY

      - name: Upload exact v3 reconciliation and trainer evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mlb-prospective-backlog-v3-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            runtime_reports/mlb_prospective_backlog_v3_latest.json
            runtime_reports/mlb_prospective_rows_v3_latest.json
            /tmp/mlb-prospective-v3-training.json
            /tmp/mlb-prospective-v3-training-invoke.json
            /tmp/mlb-prospective-v3-selection.json
            /tmp/mlb-prospective-v3-selection-invoke.json
            /tmp/mlb-prospective-v3-status.json
            /tmp/mlb-prospective-v3-status-invoke.json
