name: Inspect MLB Fix CI 30007855104

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/inspect-mlb-fix-ci-30007440962.yml'
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Print exact failed-step tail
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_ID: '30007855104'
          JOB_ID: '89208167758'
        run: |
          set -euo pipefail
          gh run view "$RUN_ID" \
            --repo "$GITHUB_REPOSITORY" \
            --job "$JOB_ID" \
            --log-failed > /tmp/mlb-fix-failed.log
          tail -160 /tmp/mlb-fix-failed.log
