name: Inqis Live Odds Pulls Disabled

# Disabled intentionally.
# Production 15-minute pull history is now AWS EventBridge -> Lambda -> DynamoDB.
# GitHub Actions must not create canonical PULLS snapshots because scheduled
# GitHub jobs are not reliable enough to be the production polling clock.
# Use the MLB All-Games Signal Proof workflow for read-only proof reports.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "GitHub production odds polling is disabled. AWS EventBridge is the source of truth."
