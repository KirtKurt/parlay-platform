name: Trigger Soccer Quota Backpressure Deploy

# Deploy the immediate lease hardening and run the full root-cause repair.
on:
  push:
    paths:
      - '.github/workflows/trigger-soccer-quota-backpressure-deploy.yml'

permissions:
  contents: read
  actions: write

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch isolated Soccer deployment
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run deploy-soccer-auto.yml \
            --repo "${GITHUB_REPOSITORY}" \
            --ref main \
            -f shared_quota_reserve_percent=0
      - name: Dispatch Soccer contention repair
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run repair-soccer-rate-limit-contention.yml \
            --repo "${GITHUB_REPOSITORY}" \
            --ref main
