name: RETIRED - Install Dedicated MLB V3 Read Lambda

# The canonical persisted-read handler is source-controlled and deployed only
# through deploy.yml. This former source-generating installer is intentionally
# impossible to dispatch.
on: []

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; use the reviewed SAM deployment workflow."
