name: MLB Live Chain PR Probe

"on":
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/mlb-live-chain-pr-probe.yml'

permissions:
  actions: read
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo probe
