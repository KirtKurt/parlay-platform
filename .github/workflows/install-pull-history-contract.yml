name: RETIRED - Install Pull History Import Contract

# Canonical pull-slot and persisted-read contracts are source-controlled and
# deployed only through deploy.yml. This former source-generating installer is
# intentionally impossible to dispatch.
on: []

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; use the reviewed SAM deployment workflow."
