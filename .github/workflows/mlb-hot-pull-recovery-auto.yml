name: MLB Hot Pull Visibility Proof Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Recovery disabled
        run: |
          echo "GitHub recovery pulls are disabled."
          echo "AWS EventBridge is the only automatic MLB pull source."
          echo "Use MLB V1 Pull Guard for read-only pull visibility."
