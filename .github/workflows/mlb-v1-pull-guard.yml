name: MLB V1 Pull Guard Legacy Manual

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy guard disabled
        run: |
          echo "Legacy scheduled Pull Guard is disabled."
          echo "The active 15-minute stale-safe heartbeat is MLB Hot Pull Visibility Proof."
